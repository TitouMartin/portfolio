{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && touch dist/.nojekyll",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && gh-pages -d dist -t true"
  },
  "dependencies": {
    "nuxt": "^3.14.1592",
    "sass": "^1.82.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
